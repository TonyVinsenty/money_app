# MoneyAPP — план и принятые решения

Обновляется по мере работы. Правила процесса — в `CLAUDE.md`, архитектурные решения — в `docs/decisions/`.

## Цель

Минималистичное, но глубокое по функциям приложение учёта доходов и расходов для Android и iOS. Сначала для себя (тесты на iPhone 14 Pro Max и на Android-телефоне), позже — публикация. Идеология: без назойливой рекламы, без «выкачивания денег»; в будущем платная подписка с дополнительными возможностями и возможность поддержать разработчика. Ориентир по удобству: Monefy (быстрый ввод), но с более сильной аналитикой и кастомизацией без подписки.

## Принятые решения (2026-09-19)

- **Local-first.** Все данные только на устройстве. Аккаунтов и синхронизации в первой версии нет.
- **Валюта:** только рубли. Код валюты хранится у каждой суммы (задел под мультивалютность).
- **Идентификаторы записей — UUID**, у записей есть `createdAt`/`updatedAt` и мягкое удаление (задел под будущий совместный бюджет).
- **Язык:** русский. **Темы:** светлая/тёмная, выбор в настройках.
- **Основной сценарий:** сделал покупку → ввёл цену → выбрал категорию (2–3 касания). Категории в подсказке упорядочиваются по частоте и времени суток (простая статистика, без ИИ).
- **Доходы и расходы** — отдельные наборы категорий. Категории по умолчанию + свои. Расходы — красная гамма, доходы — зелёная; смысл не должен передаваться только цветом (знак или иконка).
- **Баланс:** источники денег (Банк А, Наличные…). При первом запуске можно ввести текущие остатки (необязательно). Есть источник по умолчанию для расходов; остаток меняется от операций. Привязка операции к источнику необязательна, быстрый ввод остаётся «цена → категория».
- **Регулярные платежи:** календарь плановых платежей; в нужный день локальное уведомление и отметка в списке напоминаний (оплатил / пропустить / убрать). Заложить ограничения локальных уведомлений (около 64 запланированных на iOS, энергосбережение на Android).
- **Экспорт и импорт CSV** входят в первую версию (защита от потери данных при потере телефона).
- **Аналитика:** итоги за день/неделю/месяц/год/свой интервал, круговая диаграмма по категориям.
- **iOS без Mac:** разработка на Android (эмулятор, потом телефон); iOS-сборки в облаке (Codemagic или GitHub Actions на macOS). Распространение через TestFlight, если удастся оформить Apple Developer Program; иначе запасной путь — бесплатная установка своим Apple ID (7 дней, ограничения). Android-тестировщику отправляем APK.
- **Идентификатор приложения:** `com.tonyvinsenty.moneyapp`, имя пакета Dart: `money_app`.

## Этапы

| Этап | Что делаем | Результат |
|---|---|---|
| 0. Подготовка | Эмулятор, `flutter doctor`, репозиторий на GitHub, Apple Developer (параллельно, не блокирует), создание проекта | Пустое приложение запускается на устройстве |
| 1. Каркас | ADR 0001 (хранилище, состояние, пакеты), тема светлая/тёмная, навигация, тип «деньги» с тестами | Каркас проходит `analyze` и тесты |
| 2. Ядро | Локальная БД, категории, добавление дохода/расхода, история с правкой и удалением | Базовый сценарий за 2–3 касания |
| 3. Первая сборка на iPhone | Облачная сборка + установка | iPhone в деле, обратная связь |
| 4. Аналитика | Итоги по интервалам, круговая диаграмма | Экран статистики |
| 5. Баланс | Источники, стартовые остатки, источник по умолчанию | Раздел «Баланс» |
| 6. Регулярные платежи и удобство | Календарь, уведомления, подсказки категорий, экспорт/импорт CSV, настройки | Готово к ежедневному использованию |
| 7. Полировка | Крайние случаи, доступность, минимализм, бета для двоих | Версия 1.0 для себя |

Каждый этап делится на мелкие шаги по циклу: `architect` → `flutter-developer` → `/review-team` → `/checkpoint`.

## Этап 1 — шаги

Решения приняты в ADR `docs/decisions/0001`–`0004` (2026-09-19). Общий критерий каждого шага: `dart format` ничего не меняет, `flutter analyze` чист, `flutter test` зелёный, затем чекпоинт. Шаги 1.1–1.5 не добавляют пакетов.

| Шаг | Что делаем | Готово, когда (сверх общего критерия) |
|---|---|---|
| 1.1 | `flutter create --project-name money_app --platforms=android,ios .`; `applicationId` = `com.tonyvinsenty.moneyapp`; убрать демо-счётчик и шаблонный тест; проверить, что `.gitignore` не потерял записи | Приложение запускается на Android-эмуляторе, есть хотя бы один тривиальный зелёный тест |
| 1.2 | Строгий анализатор: `flutter_lints` + несколько правил под наши принципы | `flutter analyze` чист, нет подавлений через `// ignore` |
| 1.3 | Тема светлая/тёмная (Material 3), семантические цвета «расход»/«доход» (+ знак или иконка), `AppSettingsController` с `themeMode` | Widget-тест: смена `themeMode` меняет яркость темы |
| 1.4 | Русская локаль (`flutter_localizations`, `initializeDateFormatting('ru')`) | Widget-тест: `showDatePicker` показывает русские строки |
| 1.5 | Каркас навигации: `NavigationBar` + `IndexedStack` с заглушками (Записи / Аналитика / Баланс / Настройки) | Widget-тест: тап меняет вкладку, состояние вкладки сохраняется |
| 1.6 | Тип `Money` (копейки + валюта, `+`, `-`, `allocate`) | Юнит-тесты: ноль, отрицательные, большие значения, разные валюты, сумма частей `allocate` равна исходной |
| 1.7 | `parseAmount` (запятая/точка, пробелы, лишние знаки, мусор → ошибка) | Тесты на «1 234,56», «1234.5», «,5», «5,», «», «abc», «-100», очень длинное число |
| 1.8 | `DateOnly`, `Clock`, границы периодов (неделя с понедельника) | Тесты с фиксированным `Clock`: границы недели/месяца/года, февраль и 29 февраля, свой интервал включает обе границы |
| 1.9 | Форматирование денег и дат через `intl` (пакет добавляется отдельным шагом с согласием) | Тесты на русские строки; `domain/` не импортирует `intl` |
| 1.10 | Генератор id на `uuid` (UUID v7) с интерфейсом для тестов (пакет отдельным шагом) | Тест формата и возрастания id; в тестах фиксированная реализация |
| 1.11 | Документация: отметить этап 1 закрытым в этом файле | Статусы ADR и роадмап актуальны |

## Отложено (идеи, пока не реализуем)

- **Совместный бюджет** по приглашению (синхронизация между двумя пользователями): потребует серверной части; модель данных к этому готовим заранее.
- **Мультивалютность** с конвертацией по актуальному курсу.
- **Подписка** с дополнительными возможностями и «поддержать разработчика». Условия Apple/выплаты придётся выяснять отдельно.
- **Чтение банковских уведомлений:** возможно только на Android, на iOS запрещено системой; надёжнее импорт выписок (CSV/Excel).
- **Распознавание чеков:** самый дешёвый вариант — сканирование QR-кода на чеке (сумма и дата, локально); остальное — позже и, возможно, не нужно.

## Открытые вопросы

- Проверить практикой (шаг с первой базой, этап 2): работает ли `NativeDatabase.memory()` в `flutter test` на Windows без ручной установки `sqlite3.dll`.
- Apple Developer Program: регистрация не прошла проверку личности (регион аккаунта не совпал с документом), ждём ответа поддержки. Не блокирует Android-разработку.
- Подкатегории, иконки и цвета категорий: детали уточняются на этапе 2.
